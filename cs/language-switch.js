(()=>{const current=document.documentElement.lang.startsWith('cs')?'cs':'sk';const mount=()=>{if(document.querySelector('.vedator-language-switch'))return;const row=document.querySelector('.header-row');if(!row)return;let actions=row.querySelector('.vedator-header-actions');const install=document.querySelector('#installApp');if(!actions){actions=document.createElement('div');actions.className='vedator-header-actions';row.appendChild(actions);if(install)actions.appendChild(install)}const box=document.createElement('div');box.className='vedator-language-switch';box.setAttribute('role','group');box.setAttribute('aria-label',current==='cs'?'Jazyk aplikace':'Jazyk aplikácie');box.innerHTML='<button type="button" data-lang="sk">SK</button><button type="button" data-lang="cs">CZ</button>';box.querySelectorAll('button').forEach(button=>{button.classList.toggle('active',button.dataset.lang===current);button.onclick=()=>{const next=button.dataset.lang;if(next===current)return;localStorage.setItem('vedator-language',next);const url=new URL(location.href);const changed=url.pathname.replace(/\/(?:cs|sk)(?:\/index\.html)?\/?$/,`/${next}/`);url.pathname=changed===url.pathname?url.pathname.replace(/\/?$/,`/${next}/`):changed;location.href=url.href}});actions.insertBefore(box,install&&install.parentElement===actions?install:null)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount()})();
